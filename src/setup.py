from setuptools import setup

setup(
    name='teammagicsupergoal',
    packages=['teammagicsupergoal'],
    include_package_data=True,
    install_requires=[
        'flask',
        'pandas',
        'numpy'
    ],
)
